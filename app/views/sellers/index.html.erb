<p id="notice"><%= notice %></p>

<h1>Sellers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>City</th>
      <th>Postal code</th>
      <th>Thorougfare</th>
      <th>Internal comment</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Country</th>
      <th>Director firstname</th>
      <th>Director lastname</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sellers.each do |seller| %>
      <tr>
        <td><%= seller.name %></td>
        <td><%= seller.city %></td>
        <td><%= seller.postal_code %></td>
        <td><%= seller.thorougfare %></td>
        <td><%= seller.internal_comment %></td>
        <td><%= seller.email %></td>
        <td><%= seller.phone %></td>
        <td><%= seller.country %></td>
        <td><%= seller.director_firstname %></td>
        <td><%= seller.director_lastname %></td>
        <td><%= link_to 'Show', seller %></td>
        <td><%= link_to 'Edit', edit_seller_path(seller) %></td>
        <td><%= link_to 'Destroy', seller, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Seller', new_seller_path %>
